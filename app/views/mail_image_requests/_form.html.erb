<%= form_for(mail_image_request) do |f| %>
  <% if mail_image_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mail_image_request.errors.count, "error") %> prohibited this mail_image_request from being saved:</h2>

      <ul>
      <% mail_image_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>

  <div class="field">
    <%= f.label :complete %>
    <%= f.check_box :complete %>
  </div>

  <div class="field">
    <%= f.label :completed_at %>
    <%= f.datetime_select :completed_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
